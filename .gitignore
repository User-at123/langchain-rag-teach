# 密钥配置，绝不提交
.env

# 向量索引（体积大、可随时重建，不提交）
chroma_db/

# Python
__pycache__/
*.pyc
.venv/
venv/

# 大文件：亮剑扫描件原文 PDF（786MB，超 GitHub 100MB 限制，不入库）
docs/*.pdf

# OCR 结果缓存（运行时可自动重建，不提交）
ocr_cache/

# 增量索引状态文件（V8.1：记录每个文件的指纹，运行时可自动重建，不提交）
.index_state.json

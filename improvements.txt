================================================================
LangChain 教学项目 · 可改进方向与思路笔记
（基于当前项目：DeepSeek + bge-small-zh 本地嵌入 + Chroma 持久化向量库）
================================================================

一、大数据量知识库：加载与切分的提速方案
----------------------------------------------------------------
当前瓶颈：
- 整个文件 open() 一次读入内存，数据量大时直接内存爆掉
- 切分后全部存内存向量库（InMemoryVectorStore），只适合几百条以下，而且会随着电脑重启向量索引消失。
未来如果数据量变大，会导致每次启动时间花费巨大。

改进方向（按性价比排序）：
1. 换持久化向量库
   - FAISS（本地单机，轻量，pip 直接装）
   - Chroma（本地，自带简单管理）
   - 规模更大：Milvus / pgvector / Elasticsearch
   - 教学建议：先升级 FAISS 或 Chroma，改一行代码级别的成本

2. 批量加载，不要一次读入
   - 用 LangChain 的文档加载器按目录批量加载：
     DirectoryLoader / PyPDFLoader / CSVLoader / JSONLoader
   - 每种格式用对应的 Loader，而不是自己 open() 解析
   - 注意：新版 langchain-community（0.4+）已移除 DirectoryLoader 的 loader_map 参数，
     本项目已改为手动 os.walk 遍历 + LOADER_MAP 路由（见第五部分第 2 步）

3. 更优的切分策略（Chunking 优化指南）
   【切分大小的影响】
   - 切太细（如 50 字）：语义断裂。"星辰科技成立于2015年"被切成两块，
     问"哪年成立"只命中一半 -> 答错
   - 切太粗（如 1000 字）：一个块塞多个不相关主题，向量被稀释，
     区分度差 -> 噪声大，答不准
   - 经验值：中文一般 200~800 字，chunk_overlap 防止一句话卡在边界被切断
   - 判断方法：切完打印几个块看看——"一句话被拦腰切断"就调大 overlap；
     "一个块里话题混杂"就调小 chunk_size
   - 注意：改参数后必须删 chroma_db 重建索引才生效

   【不同格式的切分器对照】（本项目 2.5 步已实现其中 Markdown 与表格两项）
   | 内容类型 | 推荐切分器 | 优势 |
   | 普通文章/文档 | RecursiveCharacterTextSplitter | 通用够用（本项目现状：txt/pdf/docx） |
   | Markdown/结构化 | MarkdownHeaderTextSplitter | 按标题层级切，章节不拆散（本项目已实现：.md） |
   | 代码 | CodeSplitter | 按函数/类边界切 |
   | PDF（有章节标题） | 先解析结构再按标题切 | 保留章节层级 |
   | 表格/数据 | 一行或一个记录为一块 | 表格语义在"行"（本项目已实现：.csv/.xlsx 按行入库） |
   | 语义敏感文本 | SemanticChunker | 按语义边界切，块内主题一致，最慢但最好 |
   | 任意（进阶） | ParentDocumentRetriever | 小片段检索（精准）+ 大段落上下文（完整） |

   【切分与检索质量的关系】
   - 检索质量 = 嵌入模型质量 x 切分质量 x 检索算法
   - 切分决定"检索的最小单元"：命中准不准看块内是否完整语义；
     答得全不全看返回块是否包含完整上下文
   - 没有绝对最优参数，需要实验调参
   - 第 3 步的 BM25 + 重排序可在一定程度上弥补切分不完美（先广召回再精排）

   【当前项目的实验方法】
   - 改 rag.py 中 splitter 参数后删 chroma_db 重建索引：
     splitter = RecursiveCharacterTextSplitter(chunk_size=100, chunk_overlap=20)  # 更细
     splitter = RecursiveCharacterTextSplitter(chunk_size=400, chunk_overlap=100) # 更粗
   - 用同一批问题分别测试，对比回答质量

4. 检索质量增强
   - 混合检索：BM25 关键词检索 + 向量相似度检索，取并集再融合
   - 重排序（Rerank）：第一轮召回 Top50，用 bge-reranker 精排取 Top3，
     能显著提升命中准确率
   - 元数据过滤：给每篇文档打标签（部门/年份/类型），检索前先过滤，
     既快又准

5. 索引的增量更新
   - 知识库经常变化时，用 LangChain Indexing API 只处理新增/变更的文档，
     避免每次全量重建
   - 生产环境可用定时任务（如每天夜里重建索引）
   - 本项目方案 B（增量索引）已落地（V8.1，见第五部分"规划 B：增量索引"）：
     .index_state.json 记录文件指纹 + Chroma 按 source 增删，加新文件不再全量重建
     （含 OCR 不重跑）

二、多模态知识库：文字 + 视频 + 图片 + PDF
----------------------------------------------------------------
核心思路：把一切内容先"转成文本"，再进同一个 RAG 流程。

1. 视频内容怎么进知识库
   - 步骤：抽音频 -> 语音转文字(ASR) -> 转写文本当普通文档切分入库
   - 工具推荐：
     * 本地：faster-whisper（免费、离线、中文效果好）
     * 云端 API：阿里/讯飞/腾讯语音转写
   - 进阶：转写时保留时间戳，chunk 里带上时间点，
     这样回答时可以定位到视频第几分钟，方便跳转核对
   - 如果视频有字幕（srt 文件），直接解析字幕文本即可，最简单

2. 图片内容怎么进知识库
   - 方案 A（推荐）：图片 -> 视觉模型（Qwen-VL 等）生成文字描述 -> 入库
   - 方案 B：CLIP 类模型把图片和文字映射到同一向量空间，可直接图文互搜，
     适合"找相似图片"的场景
   - 公司海报/截图/扫描件：先 OCR 提取文字再入库
     （扫描版 PDF 已实现：V8.0 用 PyMuPDF 渲染 + RapidOCR 离线识别，见第 3.5 步；
       纯图片文件的 OCR 接入思路与此相同）

3. PDF / 表格 / 复杂排版
   - 用 unstructured 或 PyMuPDF 解析，保留表格结构和段落顺序，
     避免整页硬切导致语义断裂

4. 统一入口
   - 无论文字/视频/图片，最终都变成"文本块 + 元数据(来源、类型、时间)"，
     存进同一个向量库，检索时通过元数据区分模态

三、基于知识库做内容创作：海报、文案、建议
----------------------------------------------------------------
思路：RAG 负责"提供准确的公司素材"，生成环节负责"创作"。

1. 生成海报图片（3 种路线，由易到难）
   - 路线 A：LLM 生成文案 -> 调用绘图 API
     * 通义万相 / 文心一格 / Stable Diffusion 等文生图服务
     * 提示词 = RAG 检索到的产品卖点 + 设计风格要求
   - 路线 B（推荐做公司定制海报）：LLM 生成 HTML/CSS 模板
     -> Playwright 无头浏览器截图
     * 优点：可套公司统一模板、字体、logo，输出可编辑
   - 路线 C：程序化排版：PIL / ReportLab 按模板生成图文

2. 文案类创作（海报文案、公众号、营销话术）
   - 提示词里注入：检索到的产品资料 + 目标人群 + 平台风格
   - 注意：让 LLM 引用检索到的"事实"（如参数、价格、案例），
     创作类内容反而要给它自由度

3. 给出使用者建议（个性化推荐）
   - 做法：把用户的问题/画像也向量化，检索相关知识 ->
     让 LLM 结合资料生成针对性建议（类似"顾问模式"）
   - 进阶：沉淀用户偏好 -> 形成用户画像 -> 检索时加权

4. 公司素材（logo 等）的存储规范
   - 原则：logo 是公司静态资产，存文件系统/对象存储，不存数据库 BLOB
   - 存储位置按项目阶段划分：
     * 当前（本地教学）：项目内 assets/logo/ 目录
       PythonProject1/
       ├── assets/
       │   └── logo/
       │       ├── company_logo.png    # 透明底 PNG，合成到生成图上
       │       └── company_logo.svg    # 矢量版，报表/打印无损缩放
     * 部署后（FastAPI 服务）：云对象存储（阿里 OSS / 腾讯 COS / AWS S3），
       代码里存 URL，多实例共享 + 可挂 CDN
     * 配置方式：.env 中加 LOGO_URL=https://your-bucket.../logo.png
   - 格式建议：至少存两份（透明底 PNG + 矢量 SVG）；原始设计稿单独备份
   - 生成图片时 logo 的正确用法（关键）：
     * 不要让 AI 模型"画"logo——模型画的公司 logo 一定不准
     * 正确做法：AI 生成主体内容 + 程序化叠加真实 logo 文件
     * 按生成路线区分：
       - HTML/CSS 模板截图：logo 作为 <img> 嵌进模板（最自然）
       - 文生图 API：用 PIL 把 logo 贴到生成的图上（角落、控制透明度）
       - PIL/ReportLab 排版：直接读取 PNG 贴入
   - 注意细节：
     * 权限：对外用户与内部用户区分"带水印版/原图"
     * 备份：logo 原文件单独备份，大体积二进制不提交 git 仓库
     * 版本：文件名带版本号（logo_v2.png），公司换 logo 时历史素材仍可用

四、整体架构演进（从小脚本到产品）
----------------------------------------------------------------
1. 封装成服务：FastAPI 包一层 API，网页/小程序/IM 都能调
2. 加对话记忆：记住上下文，支持多轮追问（HistoryAwareRetriever）
3. 升级成 Agent：让模型能调用工具（查数据库、生成图片、发邮件），
   主动完成多步任务
4. 质量评估：用 RAGAS 之类的指标评估检索+生成质量，别凭感觉调参
5. 权限与安全：不同部门只能检索自己权限内的知识（元数据 + 权限过滤）

五、当前项目的最小升级路径（建议顺序）
----------------------------------------------------------------
[已完成] 第 1 步：InMemoryVectorStore -> Chroma
   - 已在 rag.py 实施：索引落盘到 ./chroma_db，首次运行创建、之后直接加载，chroma_db中存储数据被切后的数据（元数据）以及向量数据和其对应的文本块。
   - 若想换 FAISS：pip install faiss-cpu langchain-community，
     用 FAISS.from_texts(chunks, embedding) + save_local / load_local
   -如果以后想修改knowledge_base.txt，必须修改完成之后手动删除索引重建,代码为;
   Remove-Item -Recurse -Force chroma_db

[已完成] 第 2 步：多格式加载（第一阶段）
   - 已在 rag.py 实施：docs/ 目录 + os.walk 手动遍历 + LOADER_MAP 按扩展名路由
   - 背景：新版 langchain-community（0.4+）移除了 DirectoryLoader 的 loader_map 参数，
     故改为自己写循环遍历目录（for root, _, files in os.walk），不依赖框架版本 API
   - LOADER_MAP 结构：(Loader 类, 参数)，如 .txt/.md -> TextLoader(encoding="utf-8")
   - 支持 .txt / .md / .pdf / .docx / .csv，不支持的类型打印提示并跳过
   - 编码：文本类 Loader 显式传 encoding="utf-8"，避免 Windows 默认 GBK 解码中文报错
     （UnicodeDecodeError: 'gbk' codec can't decode byte ...）
   - 切分统一用 RecursiveCharacterTextSplitter，split_documents 保留来源 metadata
   - 回答时标注来源（format_docs 读取 metadata["source"]）
   - 说明：当时的已知边界（.xlsx 需另存为 CSV、切分策略未按格式区分）已在 2.5 步解决
   - 注意：docs/ 有文件时优先加载 docs/；为空时回退 knowledge_base.txt

[已完成] 第 2.5 步：多格式切分差异化（第二阶段）
   - 已在 rag.py 实施：切分阶段按扩展名路由切分器（split_documents_by_format）
     * .md   -> MarkdownHeaderTextSplitter：按 #/##/### 标题层级切，标题保留、章节不拆散；
                （注意：该切分器输入是字符串而非 Document，切出的块需手动补回 metadata["source"]）
     * .csv/.xlsx -> 表格语义在"行"：加载时已按行拆成单个 Document，切分阶段直接透传不再切
     * 其他（.txt/.pdf/.docx）-> RecursiveCharacterTextSplitter（chunk_size=200, chunk_overlap=50，保留）
   - 原生 .xlsx 支持：不用 LangChain 的 UnstructuredExcelLoader（需重量级 unstructured 依赖），
     用 openpyxl 自写 load_xlsx：每行一个 Document、单元格用 | 连接、跳过全空行、
     metadata["source"] 标注「工作表名 + 行号」（如 "sample.xlsx [工作表: 产品价格表 第2行]"）
   - 新增依赖：openpyxl
   - 验证方式：删 chroma_db 重建后，用 chromadb 直接读索引（不联网、不加载嵌入模型，
     避免 HuggingFaceEmbeddings 初始化联网卡死）确认 sample.md 按标题切成 4 块、
     sample.xlsx 按行入库 8 条（产品价格表 4 行 + 客户案例 4 行，含表头行，2.6 步已改进）
[已完成] 第 2.6 步：xlsx 表头拼接（修复"问字段名拿不到值"的 bug）
   - 背景：2.5 步按行切表后，表头行（字段名）和数据行（值）被拆成不同 Document，
     问"客户行业是什么"时检索命中表头行——只有字段名没有值，答"具体内容未显示"
   - 已在 rag.py 实施：load_xlsx 升级——第 1 行视为表头，数据行拼接为
     "字段名: 值 | 字段名: 值"（如 "客户行业: 金融 | 客户名称: 华信银行 | ..."），
     表头不再单独入库；跳过全空行、跳过与表头重复的行；
     无表头的表（表头全空）退化为原 | 连接行为
   - 原理：让数据行"自包含"字段名，检索命中数据行就能直接拿到值，不用跨块猜
   - 验证结果：加载 7 个文档（md 1 + xlsx 6）、切分 10 个片段（md 4 + xlsx 6）；
     问"客户行业有哪些"正确回答"华信银行，所属行业：金融"
   - 注意：加载/切分逻辑变化后必须删 chroma_db 重建索引才生效
[已完成] 第 2.7 步：检索参数调优（修复"列举类问题答不全"）
   - 背景：检索器 k=2 只返回最相关 2 个片段，问"所有客户的行业"只召回 1 条案例行，
     模型答"仅有一条客户案例：华信银行（金融）"
   - 试错：先试 MMR 检索（search_type="mmr", k=3, fetch_k=20）——它按"相关+多样"平衡，
     把结构相似的 3 条客户案例行当"重复"去重只留 1 条，多出的名额给了 md 里无关块，仍答不全
   - 定稿：普通相似度检索 + k=6，3 条案例行相关性都高，全部进候选
   - 验证：问"所有客户的行业有哪些？" → 完整列出华信银行（金融）、优品超市（零售）、明德学院（教育）
   - 注意：检索参数变化**不需要**重建 chroma_db（只影响查询，不影响索引内容）
   - 与第 3 步的关系：数据量变大后靠调大 k 会引入噪声，需 BM25 混合检索广召回 + reranker 精排
   - 已知边界（V7.8 记录，暂不改代码）：k=6 只是当前 10 个片段知识库的调优值，不是通用解。
     "列举所有 / 汇总 / 统计"类问题需要全量访问，不属于 Top-k 检索的舒适区。
     一万客户时两种崩法：k 太小漏召回；k 调到 1 万则提示词 token 超限 + 检索噪声爆炸。
     三档正解：整表引用（几百行）→ 分批聚合（几千~几万行）→ 结构化 RAG / Text-to-SQL
     （生产级 1 万+：客户数据进数据库，问题转 SQL 返回聚合结果，向量检索只负责定位哪张表/哪个条件）。
     结论：k=6 保留为"事实查询"教学配置；数据量真实增长到万级时走 Text-to-SQL 升级
   - 第二类边界（引用前面结果的推理，V7.8 补充）：第 1 轮"所有客户信息"走 SQL 全量导出，
     第 2 轮"基于刚才导出的第 3 条分析"——这是对上一轮结构化结果的推理，
     知识库里没有这个中间结果，走向量检索必然命中不了；正确路径是会话记忆 + 跨轮状态（第 9 步）
[已完成] 第 3 步：BM25 混合检索 + bge-reranker 重排序（第三阶段）
   - 已在 rag.py 实施：单一向量检索 → 三环节流水线
     BM25 关键词检索（jieba 分词）→ 向量检索 → RRF 融合 → bge-reranker 精排 Top 6
   - 架构变化：k 从"回答视野"（V7.7 的 k=6）降级为"粗筛漏斗"——
     两路都召回 50，精排后只留 6 条进提示词（最终视野仍承接 V7.7 的 k=6 经验）
   - 手写实现（重要教学决策）：官方 EnsembleRetriever / CrossEncoderReranker 在独立包
     langchain-retrievers，但国内镜像（清华/阿里）未同步、PyPI 也无法安装 → 装不上；
     改为手写两个类（各约 20 行）：RRFRetriever（互惠排名融合）+ RerankerRetriever
     （sentence-transformers CrossEncoder 懒加载）——教学价值反而更高
   - 踩坑 1：rank_bm25 默认按空格分词，中文整句成一个 token，BM25 直接失效
     → 必须传 jieba 分词器：preprocess_func=lambda t: list(jieba.cut(t))
   - 踩坑 2：本项目 langchain-community 版 BM25Retriever 参数名是 preprocess_func，
     不是新版独立包的 tokenizer（照抄新版文档会报 TypeError）
   - 踩坑 3：reranker 模型首次运行才下载（懒加载），import 阶段不会联网卡住
   - 验证结果：
     * BM25 问"华信银行"精准命中金融案例行；向量问"给零售客户推荐什么"语义命中优品超市
     * RRF 融合后 3 条客户案例行全部进 Top 候选
     * bge-reranker 精排后 3 条案例行稳居前 3，噪声（主营/团队介绍）被压后
     * 回归：问"所有客户的行业有哪些？" → 零售、金融、教育 ✅
   - 新增依赖：rank-bm25（BM25 算法）、jieba（中文分词）
   - 注意：检索参数变化不需要重建 chroma_db；reranker 模型首次运行自动下载
   - 与 SQL 的关系：本步只让"事实查询"更准，**替代不了 SQL**——
     全量列举/统计类问题照样 token 超限，仍走第 5 步（见 mindmap 核心概念 6"检索 vs 查询"）
[已完成] 第 3.5 步：扫描件 PDF 自动 OCR（V8.0）
   - 背景：docs/ 放入扫描版《亮剑》PDF（446 页，每页是图片、无文字层），
     PyPDFLoader 提取出 446 个空文本 Document，加载/切分正常但检索结果为空——
     "加载了 453 个文档"却"切分成 10 个片段"（空页切后仍为空）
   - 已在 rag.py 实施：load_pdf 升级为"双层判定"——
     * 第 1 步：PyPDFLoader 按页提取文字层
     * 第 2 步：逐页判定，页面文字 < OCR_MIN_TEXT_LEN(20) → 扫描页
     * 文字页直接用；扫描页：PyMuPDF 渲染成位图（dpi=200）→
       RapidOCR（离线中文识别，包内置模型）→ 按行从上到下排序拼回 Document，
       metadata 标记 "ocr": True 便于溯源
   - 设计要点：OCR 模型懒加载（只有遇到扫描页才初始化），普通文字版 PDF 不受影响；
     "逐页判定"而非"整本判定"，混合型 PDF（部分文字页部分扫描页）也正确
   - 新增依赖：PyMuPDF（渲染位图）、rapidocr-onnxruntime（离线 OCR）、Pillow（图像转换）
   - 验证：前 3 页 OCR——封面 0 行（纯图片，正常）；第 2 页"地势坤，君子以厚德载物"；
     第 3 页正文完整识别，中文质量良好
   - 注意：OCR 每页约 1~3 秒（CPU），446 页全扫描约几分钟~十几分钟，一次性；
     识别可能有少量错字但"片段匹配"下影响不大；改加载逻辑后必须删 chroma_db 重建
   - 增强（进度可视化 + 结果缓存）：OCR 循环用 tqdm 进度条——实时显示当前页/总页数、
     每页耗时、预计剩余时间，一眼看出"在正常跑"还是"卡在某页"；
     识别文本落盘 ocr_cache/<PDF 文件名>.txt（目录在 docs/ 之外，避免被 os.walk 重复加载），
     下次构建索引命中缓存（缓存 mtime ≥ PDF mtime）直接读文本，OCR 一次不重跑——
     解决"删库重建又全量 OCR 一小时"的痛点；新增依赖 tqdm
   - 教学含义：多模态内容统一思路"先转文本，再进 RAG"（见第二部分）——
     扫描件 OCR、视频语音转写、图片视觉描述都是同一条路
[已完成] 第 3.6 步：MultiQuery 多路检索（V8.2，手写）
   - 背景：问《亮剑》"李云龙妻子有几个"只答出田雨——书里明确有两位妻子（秀芹+田雨）
   - 诊断（三层实验）：①单路 RRF top50 含"秀芹"0 条——书里秀芹从不用"妻子"称呼
     （婆娘/娶媳妇/新婚妻子），"妻子"一词全命中田雨；②多路召回后若用原问题统一精排，
     秀芹仍被压到 #15（0.764 vs 田雨 0.959）；③每路用【自己的子查询】精排取 top 再合并，
     秀芹 3 条进 top6 ✅
   - 已在 rag.py 实施（手写 MultiQueryRetriever，约 90 行，零新增依赖，复用 CrossEncoder/ChatOpenAI）：
     * LLM 一次调用拆 5 路子查询（JSON 数组，三级容错解析：json.loads → 正则 → 退化原问题）
     * 每路 RRF 粗召回 top50 → 全部候选合并一个 batch 用各自子查询精排（batch_size=64，见核心概念 10）
     * 每路取前 per_query_top_n=4 → rank 融合分排序（不同路分数不可比，只比排名）→ 去重取 top6
   - 踩坑（关键）：LLM 拆的子查询不"纯"——一个子查询混 4 个名字时，秀芹被同路田雨压出前 3
     → 修 prompt：集合类问题为每个成员单独生成子查询、每路只聚焦一个实体、直接带人名
   - 验证（真实链路）：拆出"第一任妻子 杨秀芹 / 第二任妻子 田雨 / 杨秀芹关系"等 5 路；
     最终视野含秀芹 3 条；问答"两位妻子：1.秀芹（被俘牺牲）2.田雨" ✅
   - 性能优化（recall_top_n=20，V8.2 补充）：上线后问答变慢，耗时分解实测定位
     → 大头不是"多一次 LLM 调用"（仅 1.1s），是 reranker 候选 5 路×50 条=250 对
     在 CPU 上要 24.6s；每路粗召回 50→20 后 250→100 对，reranker 10.2s，
     相对单路增量 ~20s→~6s，完整问答仍答出两位妻子 ✅（详见 mindmap 核心概念 10）
   - 注意：每次问答多一次 LLM 调用（拆子查询约 1~2 秒）；万级"列举/统计"问题仍走 SQL
     （MultiQuery 让 Top-k 更"全"，但原文进提示词的物理限制不变，见 mindmap 核心概念 4）
[待做] 第 4 步：视频字幕/语音转写接入，先做最简单的 srt 解析
[已完成] 第 5 步：Text-to-SQL + 路由器（V9.0，2026-08-25 实施并验证）
   - 为什么排在 FastAPI 之前：封装应一次到位暴露完整能力；先封装纯向量版、
     后面再加 SQL 会导致接口和前端返工（"列举/统计"类问题必须走 SQL）
   - 数据库：先用 SQLite 起步（零配置、单文件，Python 内置 sqlite3），
     把 sample.xlsx 导入客户表/产品表；跑通后换 MySQL 只改连接串（SQL 语法一致）
   - 架构：LLM 路由器判断问题类型（RunnableBranch 二选一）→
     事实查询走向量链（Chroma）→ 列举/聚合/统计走 SQL 链
     （Text-to-SQL：提示词让模型把问题转成 SQL → 执行 → 结果格式化）
   - 用户视角：只暴露单一入口（门面模式），用户不知道后端有几个库；
     FastAPI 阶段依然是 POST /ask 一个接口
   - 安全：LLM 生成的 SQL 只允许 SELECT（只读白名单/最小权限），防止注入
   - 承接：V7.8 认知边界中"列举/聚合类问题 ≠ Top-k 检索"的生产级正解
   - 实际落地（对比原规划的调整）：
     * 手写 sql_db.py 代替 RunnableBranch（教学优先，零新增依赖：
       sqlite3 标准库 + openpyxl 已有，全部手写约 100 行）
     * 术语翻译不手动维护 schema 别名——"诞生时间"→"创立日期"靠 LLM 世界常识
       一次调用内化（常识推理不额外收 token）；只有捏造列名才被
       中文列名校验拦截 → 报错回传 LLM 重写 1 次 → 空结果/失败降级向量链
     * 实测 6 类问题全过：DISTINCT 列举 / COUNT 统计 / GROUP BY+ORDER BY 排名 /
       WHERE 条件 / 事实查询(vector) / 文本问题(vector)
   - 踩坑记录（详见 mindmap V9.0）：
     * format_sql_result 里 zip(cols, r) 迭代 dict 取到键不是值，结果全变列名
     * GROUP BY ... AS 中文别名被列名校验误拦 → 校验放行 AS 别名
     * 已知边界：路由器对"条件筛选 + 具体值"偶判 vector（当前数据量小能命中，
       数据量变大需补"字段条件 → sql"规则）
   - 注意事项：data.db 由 xlsx 自动导入（幂等），已加 .gitignore；
     xlsx 变更后需删 data.db 重导（SQLite 层暂无增量）
[已完成] 第 6 步：FastAPI 封装 + 极简深色前端（V10.0，2026-08-26 实施并验证）
   - 后端 app.py：POST /ask 复用 ask() 门面（零逻辑重复，门面模式价值兑现）；
     GET / 返回前端页面；GET /health 健康检查（局域网设备先探活）
   - 前端 templates/index.html：单文件内嵌 CSS/JS，GitHub Dark 极简深色风格，
     viewport 移动端适配，Enter 发送 + fetch 调 /ask 异步渲染气泡
   - 生命周期：from rag import ask 在服务启动时初始化一次（加载文档/建索引/建 SQLite），
     之后每个请求复用——RAG 重活只做一次
   - 局域网访问：python -m uvicorn app:app --host 0.0.0.0 --port 8000
     （本机 127.0.0.1；其他设备 http://<电脑局域网IP>:8000）
   - 踩坑（教学价值高）：FastAPI 同步接口跑在【线程池】，SQLite 连接在主线程创建，
     默认 check_same_thread=True → 请求报 "SQLite objects created in a thread can only
     be used in that same thread"（CLI 单线程没这问题，Web 多线程才暴露）
     → 修：sqlite3.connect(check_same_thread=False) + threading.Lock 包住 query/schema_text
   - 另：pip 需装进 .venv（项目 venv 独立于全局 Python，装错环境 import 不到）
   - 验证：冒烟脚本起服务——health 200 / 首页 200 / 空问题 400 /
     正常问题 200 且 SQL 链返回 3 行结果（金融/零售/教育）✅
   - 新增依赖：fastapi、uvicorn（requirements.txt 已加）
   - 注意事项：Windows 防火墙首次放行 8000 端口，其他设备才能访问；
     前端是"一接口一页面"最小形态，第 9/10 步接入时扩展 /ask 即可
[待做] 第 7 步：建立 assets/logo 素材目录规范          （为海报生成做准备）
[待做] 第 8 步：海报/文案生成模块（先做文案，再做图片；logo 用程序化叠加）
[已完成] 第 9 步：多轮记忆 + 跨轮状态（V11.0，2026-08-27 实施；详情见 mindmap V11.0）
   - 背景（V7.8 认知边界补充）：第 1 轮"所有客户信息"走 SQL 全量导出，
     第 2 轮"基于刚才导出的第 3 条分析"——这是对上一轮结构化结果的推理，
     知识库里没有这个中间结果，走向量检索必然命中不了；正确路径是 memory/状态
   - 会话记忆：ask() 加 session_id 参数 + 内存 sessions dict（历史保留 6 轮），
     无 session_id 时行为与 V10 完全一致（现有测试零改动通过）
   - 跨轮状态管理：SQL 结果（以 [数据库查询] 开头判定）暂存 session["last_result"]，
     引用"刚才结果/第 X 条"时 base=last_result 直接回答，不再重复查库
   - 路由器升级为三路：vector / sql / memory；历史以 history_hint 只喂路由器判断
     （不参与 vector/sql 回答，避免污染检索语义——V7.8 认知边界的落地）
   - memory 路实现：记忆补全器（memory_prompt）把指代问题改写为自包含问题 +
     JSON {rewritten, base} 判定分流（vector/sql/last_result）；解析失败降级普通路由
   - CLI 先验证：rag.py 入口改多轮循环（固定 session_id="cli"），验证指代追问
     与"引用上轮结果"两类场景；Web 同步接入：/ask 透传/生成 session_id +
     index.html localStorage 持久化（刷新页面记忆延续）
   - 零新增依赖：ChatPromptTemplate 已有 + 手写 JSON 解析 + 内存 dict 会话
   - 踩坑：memory_prompt system 里 JSON 示例 {rewritten:...} 的花括号会被 LangChain
     当模板变量 → 必须转义 {{...}}
[已完成] 第 10 步：Agent 工具化（V12.0，2026-08-27 实施；详情见 mindmap V12.0）
   - 三个工具封装：query_sql（复用 Text-to-SQL 链，成功同步 last_result）/
     retrieve_vector（复用文档检索链）/ read_memory（读会话历史 + 上轮结果）
   - 手写 ReAct 循环（_run_agent，零新增依赖不引 langgraph）：每轮把完整对话
     （SystemMessage + 历史消息 + 新 Observation）传给 LLM，MAX_AGENT_STEPS=4 防死循环；
     调用用 _llm(messages)（__call__ 等价 invoke），真实 ChatOpenAI 与测试裸 callable 兼容
   - 路由器升级四路（vector / sql / memory / agent）：保留原三路作为"快路径"，
     路由器降级为"调度员"——单跳走固定链路，多跳（先…再/结合…与…信号）进 Agent，
     Agent 超步或输出不可解析回退快路径（_run_route），异常不拖垮问答
   - 设计决策：不删除多路路由（与最初设想相反）。理由：Agent 每步 ≥2 次 LLM 调用，
     单跳问题走 Agent 更慢更贵；Agent 可能瞎编/循环/输出不可解析，固定链路是确定性
     兜底网；存量测试大量依赖路由分桶；保留两套可对比"确定性路由 vs 自主编排"
   - 场景验证（FakeLLM 链路测试）："先统计各行业客户数，再总结结果"→ Agent 调
     query_sql 真实执行 → 基于 Observation 输出 Final Answer
   - 踩坑：FakeLLM 无 invoke 方法 → 循环必须用 _llm(messages)；query_sql 工具在
     _db 为 None（注入模式）时返回提示而非崩溃
   - 测试：tests/test_agent.py 新增 6 用例（多跳编排/两工具/超步回退/解析失败回退/
     无会话兼容），pytest 34 全绿（7.26s）
[已完成] 召回修复：抽象问法具象化 + 粗召回扩容（V11.1，2026-08-27 实施；详情见 mindmap V11.1）
   - 背景：问"田雨最后的结局"答"查不到"——原文 12906 行明确写"田雨在狱中割腕自杀"，
     知识库有答案，但检索没召回（LLM 没瞎编，是防幻觉 prompt 生效 + 召回失败并存）
   - 根因（三层叠加，探针实测钉死）：
     1. 语义鸿沟：用户问"结局"（抽象词），原文写"割腕自杀"（具体词）——BM25 零匹配，
        bge-small-zh 向量被高频"田雨"主导也跨不过
     2. RRF 融合挤压：罕见词 chunk 只有 BM25 一路贡献分（1/61），
        "田雨 自杀"单路 RRF 全量排 23、"田雨 结局 自杀"排 25——被"双命中"常规 chunk 挤出
     3. 截断丢答案：recall_top_n=15 把第 25 名直接截掉；reranker 救不回没召回的东西
   - 修复（机制版，不写死"结局→自杀"映射）：
     1. MultiQueryRetriever 改写 prompt 新增"抽象问法必须具象化"规则——抽象词联想
        原文更可能出现的具体词（去世/自杀/死亡/被捕…），多个候选词分散到不同子查询；
        至少一路保留原问题原始关键词；专有名词原样带上（通用能力，换问题同样生效）
     2. recall_top_n 15→30、per_query_top_n 3→5（实测「田雨 结局 自杀」RRF 排 25，
        30 兜住；150 对精排 ≈ 15s，与旧版 20 条/路耗时相当——瓶颈在 reranker 首轮）
   - 补坑（V11.1.1，2026-08-27）：**改写随机性**——"多候选词覆盖"仍依赖 LLM 单次
     采样运气。实测「田雨和秀芹的结局分别怎么样」5 路子查询全没带"自杀"（LLM 联想
     了去世/死亡/牺牲/被捕），割腕 chunk 一路未召回答不出；而「田雨的结局」那次
     恰好联想出"自杀"就答对了——同一个抽象词，两次联想词表不同
     → 修：prompt 改为按"方向"约束（非词表）：死亡/生命状态类抽象词必须覆盖
     正常死亡（去世/病故）+ 非正常死亡（自杀/牺牲/被杀/处决/自尽）两方向各成一路
   - 教训：①只改 prompt 不够，必须同时调大粗召回漏斗（"改写质量"和"召回漏斗宽度"
     配套调）；②"让 LLM 联想具体词"要按"方向"约束而非"词表"约束——词表是补丁，
     方向是机制（每个"结局"都可能对应正常/非正常死亡两种原文写法）
   - 补坑（V11.1.2，2026-08-27）：**方向拆解通用化**——用户质疑"强化是不是只针对
     死亡类型？其他抽象词遇到同类 LLM 随机性怎么办？"确实，第 5 条只约束「结局/命运」
     类词，"婚姻/事业/战斗结果"等抽象词同样会随机漏词
     → 修：第 4 条升级为通用「方向拆解」——任何抽象/模糊词（结局/下场/命运/怎么样了/
     发生了什么/后来如何，或婚姻/事业/财产/战斗结果等）都必须先拆成 2-3 个语义方向
     （如"婚姻"→ 结婚/离婚/分居；"事业"→ 升迁/贬职/免职；"战斗结果"→ 胜利/战败/
     惨胜），每个方向用不同具体词各写一路；第 5 条死亡类降级为「方向拆解的重要案例」
     （保留强约束防回归）；新增 tests/test_multi_query_rewrite.py 断言通用方向拆解
     机制 + 非死亡类方向示例成对出现 + "不局限于本示例"防词表化
   - 验证：pytest 42 全绿（test_multi_query_rewrite.py 8 个回归测试）；
     真实 LLM 非死亡类抽象词实测探针因执行权限超时未跑成，建议手动跑
     `.\.venv\Scripts\python.exe -c "import os;from langchain_openai import ChatOpenAI;
     from rag import MultiQueryRetriever;llm=ChatOpenAI(base_url=os.getenv('OPENAI_BASE_URL',
     'https://api.deepseek.com/v1'),model=os.getenv('LLM_MODEL','deepseek-chat'),
     temperature=0.3);r=MultiQueryRetriever(base_retriever=None,llm=llm);
     print(r._generate_sub_queries('李云龙和田雨的婚姻怎么样'))"`
    期望：子查询覆盖结婚/离婚/分居/矛盾等方向
   - 补坑（V11.1.3，2026-08-27）：**最终视野扩容 top_n 6→8**——用户连续问
     「她们的结局都是什么 / 田雨的结局 / 田雨死了吗」，前两个答对、第三个答"没提到"，
     并质疑"又改回去了"（git 确认代码未回退，是真实新失败）
     → 探针钉死根因：子查询方向拆解正常（5 路覆盖死亡/自杀/被杀/关系/结局），
       割腕 chunk 在「田雨 自杀 牺牲 被杀 处决」路精排第 2（分 0.9375），
       但只在一路出现 → RRF 融合分 1/62 排第 7，top6 被"多路同现"常规 chunk
       （关系路 5 个 0.99 分）占满 → top_n=6 截断丢答案
     → 修：top_n 6→8（LLM 上下文仅多 ~1k 字）；测试 `_make()` 显式传 top_n
       并加 `top_n == 8` 断言（顺带修复测试与真实链路参数不一致的隐患）
     → 验证：同一 session 连问三题全答对；独立 session 单问「田雨死了吗」也答对
       （路由 vector，不依赖记忆）；pytest 42 全绿
   - 教训：漏斗参数三级（recall_top_n 粗召回 / per_query_top_n 每路精排 / top_n
     最终视野）任一窄都会丢答案——V11.1 调了前两级，这次才补上第三级；
     RRF 融合偏爱"多路同现"chunk，"单路高相关"chunk 融合分被稀释，
     放宽 top_n 是最低成本兜底；用户侧三题全错 vs 探针三题全对 →
     需排查 app 未重启（常驻进程不加载新代码）/会话记忆污染/LLM 单次采样随机性，
     知识库层摘要（docs/sample.md 加"田雨…狱中割腕自杀"条目）才是 100% 兜底（待拍板）
[已完成] 抽象问法具象化改造：枚举式规则 → 唤醒式预演（V12.1，2026-09-02 实施；详情见 mindmap V12.1）
   - 背景（用户点破 V11.1 的盲区）：方向拆解靠 prompt 枚举抽象词类型（结局/婚姻/事业/
     战斗结果），只对枚举过的可靠——"心情/爱情/去了哪里"等未枚举抽象说法未必触发拆解；
     "给每类抽象词补规则"根本穷尽不了
   - 认知（DeepSeek 网页版为什么不用枚举也能答好）：官方是纯推理（知识在模型脑子里）；
     本项目是 RAG，答案必须检索召回才看得到，而检索器（BM25/向量）不推理、抽象词与
     原文具体写法字面零重合——必须借 LLM 转字面词，但该用"唤醒原生联想"而非"枚举分类"
   - 改造（rag.py _generate_sub_queries 第 4 条，一行措辞的哲学转变）：
     * 改前（分类）："如果问题用了抽象词…先拆成方向"——依赖 LLM 判断"是否需拆"，词表外不积极
     * 改后（生成）："像读者一样预想原文更可能用什么具体词/情节表达这个抽象概念…凡抽象
       概括处都应如此预想"——把"是否该拆"换成"一定能预想具体写法"，覆盖任意抽象类型
     * 示例升级为类别带出：结局/命运、心情/情绪、爱情/关系、去向/下落、婚姻/事业/战斗结果，
       用 → 具体词示范，声明"不局限于本示例、随问题自由预想"防词表化
     * 保留第 5 条死亡类强锚（正常+非正常死亡双向）——实测最易漏的高价值案例，唤醒式不替代它
   - 顺带修复：app.py:31 装饰器拼写 @asynccontextmanage → @asynccontextmanager
     （少个 r 导致 import app 报 NameError、pytest 收集失败）
   - 验证：pytest 44 全绿（test_multi_query_rewrite.py 新增 2 例：唤醒式措辞 + 死亡类强锚）
   - 待办：测试用回显 FakeLLM 只锁 prompt 文本；真实 DeepSeek 是否真会拆出
     "心情→愤怒/悲痛/流泪"等方向需手动探针验证（命令见 README/improvements 旧探针改造）

[已完成] 规划 A：依赖解耦重构（V10.1，2026-08-26 实施；详情见 mindmap V10.1）
   - 动机：langchain-community 已官宣 sunset（rag.py 运行即警告）；
     BM25Retriever 参数名随版本变过（preprocess_func vs tokenizer），薄封装最易踩版本坑
   - 决策：LCEL 管道（langchain-core）不重写，保留声明式写法（见 mindmap 核心概念 8）
   - 改动清单：手写 TextLoader（open 读）/ PyPDFLoader（pymupdf get_text）/
     CSVLoader（csv.DictReader）/ Docx2txtLoader（zipfile 解 document.xml）/
     BM25Retriever（rank_bm25 + jieba，继承 BaseRetriever 保持管道兼容）
   - 配套：rag.py 顶层重活包进 init()（import 零副作用）；ask() 支持注入
     llm/retriever/db；app.py 用 lifespan 调 init()；tests/ 引入 pytest
   - 效果：langchain-community / pypdf / docx2txt 依赖全部移除，sunset 风险清零
   - 验证：pytest 24 用例全绿 + 真实链路冒烟（2541 片段不变）+ Web 启动正常

[已完成] 规划 B：增量索引（V8.1，2026-08-25 实施；详情见 mindmap V8.1 / 核心概念 11）
   - 痛点：chroma_db 存在直接加载、不存在全量重建，加新文件必须删库重建；
     扫描 PDF 每次全量重 OCR（最贵一环反复支付）
   - 改动清单：
     1. 状态文件 .index_state.json：存 {文件路径: 指纹(mtime+size)}，记录每个文件指纹
     2. rag.py 启动三向分支：无索引→全量构建；有索引无状态→全量校准（只记指纹不重嵌）；
        有状态→增量比对（新增 add_documents / 变更 delete+重建 / 删除 delete / 未变跳过）
     3. Chroma 操作：add_documents 追加、delete(where={"source": ...}) 按来源删
     4. BM25 数据源改造（关键联动）：增量后内存不再有全量 chunks，
        改为从 Chroma 全量取回文本重建——讲清"向量索引持久化 / BM25 无状态"
     5. 配套：load_documents 拆出 load_single_file（增量最小处理单元）；
        .gitignore 加 .index_state.json（可重建产物不入库）
   - 验证：临时目录 6 场景（首次全量/无变化跳过/新增/变更/删除/回归问答）全部通过；
     真实环境旧索引自动全量校准，第二次启动走增量（跳过 3 文件）；
     完整问答仍答出"两位妻子：秀芹 + 田雨" ✅

[待做] 规划 C：MultiQuery 性能优化——跨路去重等（2026-08-25 记录；第 1 项 V8.3、第 5/6/7 项 V10.2 已实施）
   - 背景：V8.2 的 recall_top_n=20 已落地（250→100 对，24.6s→10.2s），
     但"样本总量"仍有虚胖空间（见 mindmap 核心概念 10 的实测结论）
   - 关键实测数据（跨路重复率）：5 路 × 20 条 = 100 对候选，
     按 page_content 去重后只剩 53 个唯一片段（重复率 47%）——
     同一段文本被多路子查询重复召回，reranker 对相同内容算了不止一遍
   - 改动清单（按性价比排序）：
     1. ✅ 已完成（V8.3，2026-08-25）：跨路去重——收集候选时按 page_content 去重，
        100 → 53 对，实测 warm-up 后 invoke 6.5s；完整问答仍答出
        "两位妻子：田雨 + 秀芹"（细节见 mindmap V8.3；重复片段本来就会被多路
        同时取到，取一次就够）
     2. 按问题类型动态拆路：集合类（"有几个/都有谁"）才拆 5 路全覆盖，
        普通事实类 3 路就够——先让 LLM 判断问题类型，再决定拆几路
     3. reranker 轻量化/量化：bge-reranker-base 换量化版/蒸馏版（INT8 提速近翻倍），
        有 N 卡直接 GPU 推理；属硬件层，收益递减，前三项做完再考虑
     4. 两级过滤：先用 BM25/向量分数这种"免费"信号粗筛（如只留 top 30），
        再送 reranker——别把所有候选都交给最贵的环节
     5. ✅ 已完成（V10.2，2026-08-26）：并行粗召回——5 路子查询从串行改线程池并行；
        第一个子查询在主线程先跑（触发 BM25 懒索引构建，避免多线程首调重复建索引 + GIL 争抢）
     6. ✅ 已完成（V10.2）：候选减量 recall_top_n 20→15——100→75 对（去重后约 40 对），
        reranker 再省 ~2s；质量靠拆子查询带成员专有名词兜底（枚举问题每路前 15 条足够）
     7. ✅ 已完成（V10.2，新增项）：ask() 回答缓存（上限 64 条，相同问题秒回；
        注入模式不缓存，保证 mock 测试每次走真实逻辑）——重复提问场景收益最大
   - 边界提醒：以上只优化"怎么算得快"，不解决"Top-k 检索的物理天花板"——
     万级"列举/统计"问题正确答案是全集，仍走第 5 步 Text-to-SQL（不冲突）

[待做] 规划 D：多人协作知识库（登录/权限/上传/审核/分类/公网部署）
   - 目标：局域网个人问答（V10.0）→ 公网多人协作：管理员开户发号，
     员工登录后按分类上传知识，人工审核 + 模型去重后自动归档入库
   - 阶段 0（公网部署前提）：云服务器 + 域名 + ICP 备案 + Nginx 反代 HTTPS
     + systemd 常驻；uvicorn 改监听 127.0.0.1（Nginx 统一入口）；
     无公网 IP 可先用内网穿透（frp/ngrok）跑通，架构不变
   - 权限模型（管理员开户制，不开放注册）：
     * admin：全部权限 + 审核 + 批量开户 + 分类管理
     * user：问答 / 上传 / 查看自己的上传记录
     * 批量开户：数量 + 前缀 → 自动生成 emp01~empN + 随机密码 → 导出 CSV
     * JWT 认证；新增依赖：python-jose / passlib / python-multipart
   - 分类体系：categories 表（管理员后台维护）；
     docs/<分类名>/ 目录——零改动复用 os.walk 递归加载 + 增量索引
   - 上传审核流（状态机：pending → approved/deduping → dup_pending → imported/rejected）：
     * 用户选分类上传 → 落盘 uploads/pending/（扩展名白名单复用 LOADER_MAP、
       大小限制 20MB、UUID 落盘防目录穿越）
     * 管理员审核"是否录入"：驳回→通知用户（含原因）；通过→模型去重
     * 模型去重（复用 bge 嵌入 + Chroma 相似度，零新增模型）：
       ≥0.9 疑似重复→管理员裁决；0.7~0.9 相似；<0.7 新内容→自动移入 docs/<分类>/
       → 触发增量索引（只嵌新文件）
     * 站内通知（notifications 表）：用户"我的上传"见状态，管理员见待办
   - 新表：users / categories / uploads / notifications（SQLite 即可）
   - 前端：单 HTML hash 路由三视图 #/login → #/chat（问答+上传）→ #/admin（审核+开户+分类）
   - 数据库选型结论：短期 SQLite 够用（百万行内/读多写少）；新业务表直接用
     SQLAlchemy 写（将来换库只改配置）；RAG 数据量大后瓶颈在向量库，
     优先迁移 Chroma → Milvus/pgvector（排在 MySQL 之前）
   - 实施阶段：0 公网部署 → 1 登录+鉴权 → 2 批量开户+分类管理
     → 3 上传+人工审核 → 4 模型去重+裁决 → 5 自动归档+站内通知
   - 核心复用点（RAG 链路零改动）：ask() 门面 / os.walk 递归加载 / 增量索引
     / embeddings / LOADER_MAP / Chroma 相似度检索
================================================================
